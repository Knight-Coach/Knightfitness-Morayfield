{
  "name": "knightfitness-morayfield",
  "private": true,
  "description": "Knight Fitness Morayfield website. Static site, no build step. This package.json only carries the local preview server and the test suite; Vercel is told to skip installs in vercel.json.",
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "serve": "node scripts/serve.js",
    "test": "node --test \"tests/**/*.test.mjs\"",
    "test:static": "node --test tests/static.test.mjs",
    "test:browser": "node --test tests/browser.test.mjs"
  },
  "devDependencies": {
    "playwright": "1.56.1"
  }
}
